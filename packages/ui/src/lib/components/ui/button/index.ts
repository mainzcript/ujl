import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export { Root as Button, buttonVariants, type ButtonProps, type ButtonSize, type ButtonVariant };
