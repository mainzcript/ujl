# =============================================================================
# UJL Dev Demo – Environment Configuration
# =============================================================================
#
# Copy this file to .env and adjust the values as needed.
# All variables are optional – the demo works out of the box with inline storage.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Library Storage Mode
# -----------------------------------------------------------------------------
# Options:
#   - inline  (default): Library stored as Base64 in the UJLC document
#   - backend: Library stored on a Payload CMS server (services/library)
#
VITE_LIBRARY_STORAGE=inline

# -----------------------------------------------------------------------------
# Backend Configuration (only needed if VITE_LIBRARY_STORAGE=backend)
# -----------------------------------------------------------------------------
#
# To set up the backend:
# 1. Start the Library: cd services/library && pnpm run dev
# 2. Open Admin UI: http://localhost:3000/admin
# 3. Create a user (if first time)
# 4. Go to Users → Your User → Enable API Key
# 5. Copy the API key and paste it below
#
# VITE_BACKEND_URL=http://localhost:3000
# VITE_BACKEND_API_KEY=your-api-key-here
