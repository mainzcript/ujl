# =============================================================================
# UJL Library – Environment Configuration
# =============================================================================

# Required: Payload CMS secret (min. 32 characters)
# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=replace-with-your-generated-secret

# Required: PostgreSQL password
# DATABASE_URL is derived automatically in docker-compose.yml
POSTGRES_PASSWORD=replace-with-secure-password

# Optional: PostgreSQL settings (defaults work for most cases)
# POSTGRES_USER=postgres
# POSTGRES_DB=library

# Optional: Default locale for admin UI (en, de, fr, es, it, nl, pl, pt)
# PAYLOAD_DEFAULT_LOCALE=en

# Optional: Restrict CORS (comma-separated origins)
# CORS_ALLOWED_ORIGINS=https://crafter.example.com